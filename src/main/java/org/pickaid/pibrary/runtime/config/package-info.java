/**
 * Runtime adapters for Pibrary config specs.
 *
 * <p>This package binds stable config specs to Forge config, JSON resources,
 * typed datapack config families, and cached derived views for fast gameplay
 * lookup after reload.</p>
 */
package org.pickaid.pibrary.runtime.config;
