/**
 * Shared state keys and sync mode descriptors.
 *
 * <p>This package names state fields and their intended sync policy. Encoding,
 * transport, and persistence are handled by the layers that consume these
 * descriptors.</p>
 */
package org.pickaid.pibrary.api.state;
