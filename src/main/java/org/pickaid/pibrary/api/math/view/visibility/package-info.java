/**
 * Reusable visibility classification helpers for Pibrary math.
 *
 * <p>Use this package right after projection when later code wants a clean
 * answer like visible, offscreen, behind camera, or not projectable. That
 * keeps render code, UI code, and debug code from each inventing slightly
 * different visibility rules.</p>
 */
@org.jetbrains.annotations.ApiStatus.Experimental
package org.pickaid.pibrary.api.math.view.visibility;
