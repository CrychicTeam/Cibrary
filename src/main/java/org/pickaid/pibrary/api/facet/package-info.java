/**
 * Facet annotations, descriptors, and typed handles.
 *
 * <p>Use living facets for state attached to entities, level facets for state
 * attached to a dimension, and chunk facets for data owned by loaded chunks.
 * Facet classes are registered once and then used as the only public handle for
 * resolving instances.</p>
 */
package org.pickaid.pibrary.api.facet;
