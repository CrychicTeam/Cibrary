/**
 * Facet annotations, descriptors, and typed handles.
 *
 * <p>Use living facets for state attached to entities and level facets for
 * state attached to a level or dimension. Facet classes are registered once and
 * then used as the only public handle for resolving instances.</p>
 */
package org.pickaid.pibrary.api.facet;
