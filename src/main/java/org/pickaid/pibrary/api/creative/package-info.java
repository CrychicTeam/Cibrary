/**
 * Creative-tab content helpers.
 *
 * <p>The stable layer is deliberately data-first: entries can be grouped by a
 * Pibrary section, expanded into item-stack variants, and assigned display or
 * search visibility before a loader-specific adapter writes them into the real
 * Minecraft creative tab.</p>
 */
package org.pickaid.pibrary.api.creative;
