/**
 * Minimal sync bridge contract used by Pibrary hosts.
 *
 * <p>This package keeps the public boundary small. Runtime transport and packet
 * implementation stay outside the core API.</p>
 */
package org.pickaid.pibrary.api.sync;
