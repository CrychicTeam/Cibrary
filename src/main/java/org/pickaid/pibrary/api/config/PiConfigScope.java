package org.pickaid.pibrary.api.config;

/**
 * Storage and ownership scopes supported by config entries.
 */
public enum PiConfigScope {
    COMMON_BOOTSTRAP,
    SERVER_DATA_PACK,
    CLIENT_LOCAL
}
