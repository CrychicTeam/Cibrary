/**
 * Small config descriptors shared by Pibrary systems and examples.
 *
 * <p>This package describes config entries, grouped specs, typed values, scopes,
 * comments, and validation. It does not force a complete file loader, so the
 * same spec can feed Forge config, datapack JSON, local client files, or
 * generated docs.</p>
 */
package org.pickaid.pibrary.api.config;
