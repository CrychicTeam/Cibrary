/**
 * Small config descriptors shared by Pibrary systems and examples.
 *
 * <p>This package describes config entries, grouped specs, typed values, scopes,
 * comments, validation, and typed datapack config families. The same entry spec
 * can feed Forge config, datapack JSON, local client files, or generated docs;
 * {@link org.pickaid.pibrary.api.config.PiDataConfigType} and
 * {@link org.pickaid.pibrary.api.config.PiDataConfigEntry} cover
 * registrate-style content config such as one datapack file per spell.</p>
 *
 * <p>The typed datapack config direction learns from lcy's L2 project family,
 * especially L2Core's config type and datagen collector flow.</p>
 */
package org.pickaid.pibrary.api.config;
