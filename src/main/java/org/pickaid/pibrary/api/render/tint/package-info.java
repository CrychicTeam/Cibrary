/**
 * Client-side model tint helpers.
 *
 * <p>This package wraps Minecraft's normal {@code BlockColor} and
 * {@code ItemColor} systems. Most mods should use the short methods on
 * {@code PiBaseRegistrate}, such as {@code tintFoliage(...)} or
 * {@code tintDurability(...)}. The builder and provider helpers are for models
 * with several tint layers or colors read from stack/block state data.</p>
 */
package org.pickaid.pibrary.api.render.tint;
