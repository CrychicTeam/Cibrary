# Pibrary 包结构说明

[English](PACKS.md)

这个文件说明 `Pibrary` 在从旧单体拆分成新仓库家族的过程中，当前应如何保持整洁。

## 永久保留在本仓库中的内容

这些职责长期保留在 `Pibrary`：

1. 公共核心契约；
2. service 定位与基础抽象；
3. 配置、诊断、注册、状态、目标选择、数学 helper 等通用边界；
4. 实体生命周期、projectile trace，以及服务可选 compat 层的 JEI 中立契约；
5. 作为根核心模组的可迁移启动层。

## 独立基础仓库

以下内容即使与 `Pibrary` 强相关，也应保持为独立仓库：

1. `PiNet`
2. `PiSerializeKit`
3. `PiKubeJSCompat`
4. `PiJEICompat`
5. `PiDataGraph`

原因是它们具备跨项目复用价值，不应要求消费者必须携带整个 `Pibrary`。

## 引擎仓库

以下内容属于引擎家族，不应继续塞回根核心仓库：

1. 相机系统；
2. UI / HUD / 动画界面系统；
3. 剧情 / 对话 / 任务系统；
4. 动画与渲染桥系统；
5. 实体特效与数据图玩法引擎。

## 旧代码处理

旧单体快照保留在：
`restore/legacy-monolith-20260331`

当前规则是：

1. 保留它用于迁移和回看；
2. 不再把它当作目标架构；
3. 先把可复用部分提炼成稳定 API，再把实现迁往独立仓库。
4. 对象计数与图驱动逻辑迁往 `PiDataGraph`，不要再回流进 `Pibrary`。
